groups:
- name: llm-alerts
  rules:
  - alert: LLMHighLatency
    expr: histogram_quantile(0.90, rate(llm_generation_latency_seconds_bucket[5m])) > 3
    for: 2m
    labels:
      severity: critical
    annotations:
      description: "LLM latency is above 3 seconds"
